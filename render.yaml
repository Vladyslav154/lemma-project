services:
  - type: web
    name: lemma-project
    env: python
    plan: free # Или ваш платный тариф
    pythonVersion: "3.11"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    healthCheckPath: /?lang=ru